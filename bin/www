#!/usr/bin/env node
var debug = require('debug')('proctor-server');
var server = require('../server');

var port = process.env.PORT || 3000;

server.listen(port, function() {
    debug('listening on port %d', server.address().port);
});